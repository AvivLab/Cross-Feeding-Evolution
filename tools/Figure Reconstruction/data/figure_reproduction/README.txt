Place batch_hit_counts.csv here, or build it with:
  python3 scripts/build_summary_hit_counts.py --output-root OUTPUT_ROOT
  python3 scripts/assemble_figure_data.py --output-root OUTPUT_ROOT
See README.md.
