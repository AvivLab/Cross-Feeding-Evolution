# Tkinter is in the stdlib but needs a Python build with Tcl/Tk (e.g. on macOS Homebrew:
#   brew install python@3.12 python-tk@3.12
# The PyPI "tk" package is unrelated; do not use it as a substitute for tkinter.)
numpy>=1.23
numba>=0.59
matplotlib>=3.7
scipy>=1.10
pandas>=2.0
openpyxl>=3.1
ttkbootstrap>=1.10
diptest>=0.5.0
psutil>=5.9.0
scikit-learn>=1.0.0
umap-learn>=0.5.0